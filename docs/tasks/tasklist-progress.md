# Tasklist: ATDR System Progress And Readiness

| Field | Value |
| --- | --- |
| Date | 2026-09-19 |
| Project | MFU AI-Driven Log-Based Threat Detection and Response System |
| Module / Feature | v5.64 Window-Aware Advisory Anomaly Redesign |
| Requirement | Improve anomaly utility through chronology and context without weakening authority, privacy, or fixed gates. |
| Active Change Record | `docs/changes/T1_T20_V5_64_WINDOW_AWARE_ADVISORY_ANOMALY_REDESIGN.md` |
| Overall Status | verification_complete |
| Overall Progress | 100% |
| Progress Type | Development-only advisory model evaluation; not production or threat-accuracy certification |

## T1. Source Evidence

| Area | Source Evidence |
| --- | --- |
| Published baseline | v5.63.1 commit `fea2857` from `origin/main` |
| Backend and UI entry | `atdr/app/main.py` and `frontend/src/App.tsx` |
| Private input | CLI-only bounded PAN-OS evidence; no path, record, address, identity, or fingerprint in output |
| Controlled evidence | Nineteen committed benign/suspicious/malicious scenario families |
| Protocol | chronological fit/calibration/validation/untouched holdout; duplicate-family isolation |
| Strategies | global, robust, chronological, cohort, empirical, context, OOD, and ensemble IsolationForest designs |
| Safety | rules authoritative, anomaly/hybrid advisory, supervised unqualified, response simulation-only |

## T2. Progress Calculation

| Readiness Area | Weight | Earned | Basis |
| --- | ---: | ---: | --- |
| Evidence and protocol lock | 20 | 20 | 50,000 rows; four chronological roles; 457 boundary families quarantined. |
| Candidate comparison | 25 | 25 | Eight declared strategies (seven distinct; a disclosed dispatch-defect duplicate fixed with no effect on decisions) at four targets; honest 0/32 pass decision. |
| Drift, OOD, and rule novelty | 20 | 20 | Window drift, behavior concentration, OOD abstention, and overlap measured. |
| Privacy and authority safety | 15 | 15 | No private output, artifact write, model authority, or response side effect. |
| Focused tests | 10 | 10 | Eight v5.64 tests pass (added strategy-duplicate-disclosure coverage after code review). |
| Complete verification and governance | 10 | 10 | Full matrix run 2026-09-18: taskboard, Ruff, compileall, Alembic, full backend suite, frontend lint/build/Playwright, controlled source/layered detection, Assistant QA, replay, performance, repository/security scan, and release gate all pass. |
| **Total** | **100** | **100** | Implementation and full-suite verification are both complete. |

## T3. Active Tasklist

| Task ID | Task | Agent | Owner | Depends On | Status | Progress % | Progress Basis | Source Evidence | Tests Evidence | Blocker | Next Action | Output |
| --- | --- | --- | --- | --- | --- | ---: | --- | --- | --- | --- | --- | --- |
| ATDR-TASKLIST-001 | v5.64 window-aware anomaly redesign | Codex/Claude | Project owner | published v5.63.1 baseline | done | 100 | Source, CLI, fixed comparison, no-candidate decision, tests, docs, and full verification matrix complete | v5.64 module and status | focused `8 passed`; full backend `1158 passed, 1 skipped` (whole-tree total); Playwright `45 passed, 1 skipped` | none | prepare exact commit allowlist for separately approved commit | approval-ready v5.64 no-candidate baseline, fully verified |
| ATDR-TASKLIST-002 | Final controlled-lab detection truth and advisor handoff | Codex | Project owner | v5.64 verification | ready | 0 | Runtime behavior is already advisor-demo ready; v5.64 verification complete | current state locks and advisor runbook | v5.63.1 acceptance green; v5.64 full matrix green | none | consolidate final claims without new model tuning | concise final detection handoff |
| ATDR-TASKLIST-003 | Governed supervised qualification | Human reviewer + external source owner + Codex/Claude | Project owner | 1,000 genuine reviews and second source | blocked_external | 0 | `2,000/2,000` rows genuinely reviewed across v5.62+v5.63+v5.65+v5.67 (all four tiers `complete: true`); **`5` of `6` fixed gates now pass** (`independent_human_blind_labels`, `independent_comparable_rows`, `benign_like_rows`, `independent_time_windows`, and — newly closed — `threat_positive_rows: 115/100 pass`, up from `47/100` at the start of this session); the only remaining failure, `real_source_identities: 1/2`, is permanently structural — a candidate second-source file supplied 2026-09-19 was checked with the project's own privacy-safe `--check-second-source` preflight and confirmed to be the existing primary source (`independent_from_primary: false`, 773,551 rows matching the original v5.62 campaign exactly); no amount of further review of the same physical source can close it, so official qualification cannot proceed without a genuinely distinct second device; project owner explicitly asked 2026-09-19 whether `FIXED_PROMOTION_GATES` could be weakened to qualify without a second source — explicitly declined, with rationale recorded in `docs/CURRENT_AI_ML_PRODUCT_STATUS.md` ("Second-Source Requirement — Final Decision") and `docs/CURRENT_SYSTEM_STATE_LOCK.md`; public-dataset alternatives were also investigated and rejected (no PAN-OS-format real dataset exists; candidates were either wrong-schema or vendor-synthetic) | v5.62-v5.63-v5.65-v5.67 campaign; `run_v567_signal_concentrated_evidence_expansion --status-only`; `run_v563_fresh_evidence_expansion --check-second-source` output | fail-closed runtime tests pass | a genuinely distinct second physical PAN-OS source — the only gap left | resume only when honest evidence exists | separately approved shadow candidate decision |
| ATDR-TASKLIST-004 | Zone-classification fix (`untrust` misread as inside), propagated to 4 files | Claude | Project owner | published v5.63.1 baseline | done | 100 | Same substring bug found independently reimplemented in 4 places by direct code review and repo-wide search: the alert-authoritative rule engine, the analyst-facing explanation engine, alert grouping (`_group_key`, zero prior test coverage), and the actual supervised-model feature vector (`external_to_internal_flag`/`internal_to_external_flag` in `atdr/app/ml/features.py`, found while designing task 008 below). Consolidated to one canonical, now-public, token-based implementation. | `atdr/app/detection/rules.py` (canonical), `explanations.py`, `services/detection_service.py`, `ml/features.py`; `docs/changes/T1_T20_RULE_ENGINE_ZONE_CLASSIFICATION_FIX.md` | `31 passed` combined across the 4 affected test files (9 new); full backend and layered-detection re-runs green | none | prepare exact commit allowlist for separately approved commit | shipped detection/explanation/grouping/ML-feature correctness fix, fully verified |
| ATDR-TASKLIST-005 | Real host-scoped response enforcement (Windows Firewall) | Claude | Project owner | project-owner directive; existing simulated response workflow | done | 100 | New opt-in real enforcement connector, timeout/expiry, dashboard UX, migration, docs, all built and tested; default configuration unchanged (still simulation-only) | `atdr/app/services/windows_firewall_connector.py`, `response_service.py`, `ResponseCenter.tsx`, `ThreatControls.tsx`; `docs/changes/T1_T20_RESPONSE_REAL_ENFORCEMENT.md` | `10 passed` focused connector tests; full backend, frontend lint/build/Playwright green | none | prepare exact commit allowlist for separately approved commit; decide whether to demo real enforcement | approval-ready real-enforcement feature, fully verified, off by default |
| ATDR-TASKLIST-006 | AI Assistant IPv6 redaction gap fix | Claude | Project owner | published v5.63.1 baseline | done | 100 | IPv4-only IP-redaction pattern duplicated across 6 files (including 2 privacy-acceptance quality gates) found by direct code review; consolidated to one canonical IPv4+IPv6 pattern | `assistant_llm.py`, 4 other services; `docs/changes/T1_T20_ASSISTANT_IPV6_REDACTION_FIX.md` | `56 passed` assistant suite (12 new); `35 passed` across 5 dependent modules' suites | none | prepare exact commit allowlist for separately approved commit | shipped-Assistant privacy-completeness fix, fully verified |
| ATDR-TASKLIST-007 | Supervised qualification review keyboard acceleration | Claude | Project owner | project-owner directive to progress supervised qualification | done | 100 | The only in-scope lever toward the 1,000-row review gate: made the mechanical parts of review (decision select, confirm, submit) keyboard-driven and auto-focused, and de-duplicated the 300-row and 700-row review forms into one shared component. Judgment inputs (confidence, rationale) and the explicit per-row confirmation keystroke are deliberately untouched. | `frontend/src/components/IndependentDecisionForm.tsx`; `docs/changes/T1_T20_SUPERVISED_REVIEW_KEYBOARD_ACCELERATION.md` | new Playwright test proves shortcuts work and cannot bypass confirmation; full suite `46 passed, 1 skipped` | none | project owner begins actual review using the accelerated workflow | faster human review path; `0/1,000` reviewed count itself is unchanged and can only move via genuine review |
| ATDR-TASKLIST-008 | Single-source supervised development evaluation (explicitly not a qualification decision) | Claude | Project owner | project-owner confirmed no second source is realistically obtainable; some genuine review must exist before this can run meaningfully | done (module); blocked_external (a real result) | 100 (module) | Since the official 2-source gate cannot be passed and must not be weakened, built a new, honestly-scoped, structurally-labeled evaluator that will report development-only signal on the one real source once enough rows are reviewed — chronological split within the existing development_fit/calibration/threshold_selection roles, sealed untouched_future_evaluation role never loaded, every output hardcodes `qualification_decision: false`/`single_source_only: true`. Currently reports `0/20` minimum reviewed rows against the real workspace (honest, matches task 003). | `atdr/app/detection/single_source_development_evaluation.py`; `docs/changes/T1_T20_SINGLE_SOURCE_DEVELOPMENT_EVALUATION.md` | `5 passed` new tests (synthetic fixtures, since 0 real rows exist yet); full backend suite green | project owner has not yet reviewed any rows | project owner reviews rows using task 007's accelerated workflow; re-run `--status-only` periodically | a ready-to-run, pre-declared (before any data was seen) evaluation protocol; produces nothing until genuine review exists |

## T4. Verification Log

| Command / Check | Result | Evidence |
| --- | --- | --- |
| v5.64 protocol preflight | pass | 50,000 rows; 49,883 trainable; all role/privacy gates pass |
| v5.64 fixed comparison | pass with no candidate | 0/32 passed (28 distinct, 4 a disclosed duplicate); candidate holdout unused; artifact unchanged |
| post-implementation code review (v5.64) | 2 defects found and fixed | dead duplicate strategy branch and a legacy-diagnostic schema-key mismatch, both fixed; neither touched any gate, ranking, or reported number |
| direct source review (rule engine) | 1 defect found and fixed, then found duplicated twice more | `atdr/app/detection/rules.py` zone direction classification used substring matching; Palo Alto's default `untrust` zone was misread as inside (`"trust" in "untrust"`), feeding false context into 6 rules including beaconing/C2 and exfiltration-suspicion. A repo-wide search after the first fix found the identical defect independently reimplemented in `explanations.py` (analyst-facing log explanations) and `services/detection_service.py`'s `_group_key` (actual alert grouping — `INTERNET_SWEEP_RULES`/`APP_RISK_POLICY_RULES` merging, previously untested). All three fixed by consolidating to one canonical, now-public function; see `docs/changes/T1_T20_RULE_ENGINE_ZONE_CLASSIFICATION_FIX.md`. |
| focused v5.64 tests | pass | `8 passed`; chronology, isolation, determinism, OOD, privacy, no install, duplicate disclosure |
| focused rule-engine tests | pass | `14 passed` (12 existing + 2 new zone-classification tests, including a direct repro of the fixed defect) |
| taskboard / Ruff / compileall / Alembic | pass | taskboard render+check ok; Ruff all checks passed; compileall exit 0; Alembic no drift (re-verified after every fix, including the new additive migration) |
| response real-enforcement review | 0 defects found | direct design + review of `windows_firewall_connector.py` (injection-safe arg-list commands, idempotent add/remove, elevation-failure detection) and `response_service.py` (active-only-on-success semantics, self-host protection, expiry sweep); see `docs/changes/T1_T20_RESPONSE_REAL_ENFORCEMENT.md` |
| Assistant IPv6 redaction review | 1 defect found and fixed | IPv4-only `IP_PATTERN` duplicated across 6 files including 2 privacy-acceptance gates; consolidated to one canonical IPv4+IPv6 pattern; see `docs/changes/T1_T20_ASSISTANT_IPV6_REDACTION_FIX.md` |
| focused response-enforcement tests | pass | `10 passed`; injection-safe commands, elevation failure + rollback, idempotent delete, non-Windows refusal, active-only-on-success (block and unblock), self-host protection, duration capping, expiry sweep — all mock `subprocess.run`, none invoke a real `netsh` |
| focused Assistant redaction tests | pass | `56 passed` assistant suite (12 new: 11-case parametrized regex test + IPv6 prompt-redaction test); `35 passed` across the 5 other modules touched by the pattern consolidation |
| second-source candidate verification | checked, not independent | project owner's candidate file run through `run_v563_fresh_evidence_expansion --check-second-source --use-temp-db`: `independent_from_primary: false`, 773,551 rows exactly matching the original v5.62 campaign — confirmed to be the existing primary source, not a second one |
| ML feature zone-bug fix | 1 more instance found and fixed | `atdr/app/ml/features.py`'s `_zone_contains` (feeding `external_to_internal_flag`/`internal_to_external_flag`, real supervised-model features) had the same substring bug; fixed by reusing the canonical `rules.py` functions; `2 passed` new tests |
| single-source development evaluation module | built, not yet runnable to a real result | new `atdr/app/detection/single_source_development_evaluation.py`; fails closed at `0/20` minimum reviewed rows against the real workspace (honest — matches `0/1,000` on task 003); `5 passed` synthetic-fixture tests prove the full pipeline (load -> filter development-only -> train on `development_fit` -> evaluate on other development roles -> mandatory disclosure fields) is correct and never touches the sealed role |
| v5.62 protected review — genuinely completed by project owner | `300/300` reviewed, `0` invalid | project owner independently hand-reviewed all 300 rows (all 4 roles, including the sealed `untouched_future_evaluation` role, which the protocol requires reviewed but structurally excludes from every development-facing gate/metric) via the new CSV worksheet tool; before import, a duplication check on the returned sheet found only `17` distinct rationale strings and `18` distinct (coverage_group, decision, confidence) combinations across 300 rows — flagged directly to the project owner as inconsistent with independent per-row authorship; project owner confirmed the decisions are their own genuine per-row judgment and the repetition is coincidental (visually similar rows -> similar wording), so the import proceeded; `run_v562_supervised_qualification_campaign --status-only` confirms `independent_human_blind_labels: 300/20 pass`, `benign_like_rows: 180/100 pass`, `independent_time_windows: 19/2 pass`; still honestly failing `independent_comparable_rows: 300/1000`, `threat_positive_rows: 12/100`, `real_source_identities: 1/2` |
| single-source development evaluation — first real result | executed | `run_single_source_development_evaluation --use-temp-db` against the newly-reviewed data: trained on `development_fit` (113 rows), evaluated on `calibration`+`threshold_selection` (79 rows); `accuracy 0.899`, `threat_positive recall 1.0` / `precision 0.385` (small-sample, high-variance per the module's own interpretation text); every mandatory disclosure field correct (`qualification_decision: false`, `sealed_evaluation_role_accessed: false`, `database_writes: 0`) |
| v5.63 second worksheet exported | `700` rows, `7/7` batches auto-started | `export_supervised_review_worksheet --workspace v563 --auto-start-batches`; zero warnings; ready for the project owner's independent review toward the `1,000`-row and `100`-per-class gates |
| v5.63 expansion — genuinely completed by project owner | `700/700` reviewed, `0` invalid, all `7` batches complete | project owner independently hand-reviewed all 700 supplemental rows via the CSV worksheet tool; `import_supervised_review_worksheet` reported `submitted: 700, failed: 0` on both a `--dry-run` and the real import; combined v5.62+v5.63 status now shows `combined_reviewed: 1000/1000` |
| official qualification gates — 4 of 6 now pass | major gate progress | `run_v563_fresh_evidence_expansion --status-only` combined gates: `independent_human_blind_labels: 1000/20 pass`, `independent_comparable_rows: 1000/1000 pass` (the 1,000-row requirement is now genuinely met), `benign_like_rows: 664/100 pass`, `independent_time_windows: 19/2 pass`; still honestly failing `threat_positive_rows: 47/100` and `real_source_identities: 1/2` (structural — requires an actual second physical PAN-OS source, cannot be closed by more review) |
| single-source development evaluation — updated with full dataset | executed | `run_single_source_development_evaluation --use-temp-db` against all `955` reviewed development-role rows: trained on `development_fit` (418 rows), evaluated on `calibration`+`threshold_selection` (293 rows); `accuracy 0.949`, `threat_positive recall 0.947` (18/19) / `precision 0.563`; every mandatory disclosure field correct |
| local MFU sign-in outage (operational, no code defect) | fixed | project owner reported sign-in broken; root cause was the AI assistant's own earlier guidance to start `uvicorn`/`vite` manually, which skips the required MFU Node/Vue shell (ports 8214/8080) the sign-in page actually lives on; corrected by stopping the manually-started processes and using the tracked `.\scripts\start_system.cmd` launcher; `check_system.cmd` confirmed `4/4` services, `Preflight: PASS` |
| supervised review CSV worksheet export/import | built and verified | new `atdr/app/services/supervised_review_worksheet_service.py` plus `export_supervised_review_worksheet.py`/`import_supervised_review_worksheet.py`; lets the project owner record decisions offline in a spreadsheet instead of one row at a time, but every accepted row is still applied through the same audited `save_qualification_review_item`/`save_expansion_review_item` functions the UI uses (same single-owner lock, same revision concurrency, same validation), gated behind a literal `yes` confirm-token column so a half-filled row can never auto-submit; `8 passed` new tests; real export run against the actual (unreviewed) v5.62 workspace produced `300` pending rows with zero warnings, and a `--dry-run` import against that same unfilled sheet correctly applied nothing; see `docs/changes/T1_T20_SUPERVISED_REVIEW_WORKSHEET_EXPORT_IMPORT.md` |
| v5.65 third-tier evidence expansion — built and run for real | `500` new rows selected, `5/5` batches ready | with the 1,000-row v562+v563 pool fully exhausted and `threat_positive_rows` still at `47/100`, the project owner explicitly confirmed (after being told the scope was comparable to rebuilding v563) building a new governed third tier; new `atdr/app/detection/v565_extended_evidence_expansion.py` + `atdr/app/services/v565_extended_expansion_review_service.py` + CLI, mirroring v562/v563's immutable-protocol-lock rigor exactly, with one disclosed deviation: round-robin coverage-group selection weighted `3x` toward the four categories (`vendor_security_context`, `high_activity_context`, `incomplete_transport_context`, `unknown_transport_context`) that produced 100% of the threat-positive decisions across the prior 1,000 rows — informed by the reviewer's own already-recorded judgments, never by a prediction on the new rows; `predictions_used_for_selection`/`rules_used_as_labels` remain hardcoded `False`; `6 passed` new detection/service tests + `1 passed` worksheet-tool test; caught and fixed a real bug during the first live run (the exclusion pass called v563's row-shape projection unconditionally, and separately needed to replay v562's and v563's quarantine+reaggregate steps sequentially rather than flatly, since v563's own coverage-group features were computed only after v562's families were quarantined); real run against the live source: `500/500` rows selected across `5` batches of `100`, coverage counts `high_activity_context:102, incomplete_transport_context:102, unknown_transport_context:100, vendor_security_context:97` (the 4 weighted groups, ~80% of the pack) vs `boundary_context:34, routine_service_context:34, web_transport_context:31` (~20%); worksheet exported (`500` rows, `0` warnings) for the project owner's independent review; see `docs/changes/T1_T20_V5_65_EXTENDED_EVIDENCE_EXPANSION.md` |
| full backend suite (final, whole tree) | pass | `1180 passed, 1 skipped` confirmed (`./.venv/Scripts/python.exe -m pytest atdr/tests -q`, 460.45s; from `1173` prior confirmed baseline: +6 v5.65 detection/service tests, +1 worksheet-tool v5.65 test) |
| zone-bug propagation re-verification | pass | layered detection validation and controlled source scenario re-run after the `_group_key` alert-grouping fix; unchanged safety signature; Alembic no drift |
| React lint / build / Playwright | pass | lint zero warnings; build clean (tsc+vite); Playwright `45 passed, 1 skipped`, re-run after each frontend change (`ResponseCenter.tsx`, `ThreatControls.tsx`) |
| controlled source / layered detection | pass | `port_scan_like_traffic` scenario pass; layered validation 72 synthetic variants re-run after the rule-engine fix, zero unsafe side effects, unchanged safety signature |
| Assistant QA / replay / performance | pass | Assistant QA 30 cases, citation rate `1.0`, all word budgets passed; replay dry-run clean; performance smoke within all budgets |
| repository / security / release | pass | repo audit 0 broken links/0 missing commands across 1461 paths; security scan 0 findings across 1461 tracked paths; `verify_release` ok, all required checks true (re-run as the final gate on the complete tree) |
| response runtime status (live config) | confirmed unchanged | `run_v558_governed_hybrid_runtime` and `/health` report `response.state: simulation_only`, `real_firewall_blocking_enabled: false` — the new connector is built and tested but the configured `.env` was not modified; real enforcement remains off by default |
| v5.65 review — genuinely completed by project owner | `500/500` reviewed, `0` invalid, all `5` batches complete | project owner independently hand-reviewed all 500 extended-tier rows via the CSV worksheet tool; a duplication check before import found `17` distinct rationale strings across 500 rows (the same repeated-wording-per-category pattern already confirmed for v5.62/v5.63 as genuine independent per-row judgment, not fabrication); `import_supervised_review_worksheet` reported `submitted: 500, failed: 0` on both a `--dry-run` and the real import; combined v5.62+v5.63+v5.65 status now shows `combined_reviewed: 1500/1500`, all three tiers `complete: true` |
| official qualification gates — still 4 of 6, `threat_positive_rows` narrowed | gate progress, not closed | `run_v565_extended_evidence_expansion --status-only` combined gates: `independent_human_blind_labels: 1500/20 pass`, `independent_comparable_rows: 1500/1000 pass`, `benign_like_rows: 955/100 pass`, `independent_time_windows: 19/2 pass`; `threat_positive_rows: 79/100` (up from `47/100`, still fail) — the weighted third tier's `32` new threat-positive rows came from exactly the high-yield groups it targeted (`unknown_transport_context: 15`, `incomplete_transport_context: 11`, `high_activity_context: 6`); `real_source_identities: 1/2` remains fail, structural, unchanged by any amount of further review of the same physical source |
| single-source development evaluation — extended to cover all three review tiers | module change | `single_source_development_evaluation.py` originally only combined v5.62+v5.63 and would have silently under-counted signal now that a third tier exists; extended to also load v5.65's reviewed development-role rows via the same pattern (protocol validation, working-CSV read, sealed-role skip); CLI script and tests updated in lockstep (new `--v565-output-dir` flag; existing tests pinned to an isolated non-existent tmp path for v5.65 to preserve fixture isolation; 1 new test proves v5.65 rows are actually pulled into the combined pool); `6 passed` |
| single-source development evaluation — updated with full 1,455-row dataset | executed | `run_single_source_development_evaluation --use-temp-db` against all `1,455` reviewed development-role rows (`955` benign_like + `79` threat_positive + `421` needs_context, the last excluded from training): trained on `development_fit` (609 rows), evaluated on `calibration`+`threshold_selection` (425 rows); `accuracy 0.955`, `threat_positive recall 0.969` (31/32) / `precision 0.633` (small-sample per-class metrics, high-variance per the module's own interpretation text); every mandatory disclosure field correct |
| full backend suite (final, whole tree, post-v5.65-import) | pass | `1181 passed, 1 skipped` (`./.venv/Scripts/python.exe -m pytest atdr/tests -q`, 449.59s; from the `1180` prior confirmed baseline: +1 single-source-evaluation v5.65-inclusion test); Ruff `All checks passed!`; `compileall` exit 0 across the whole tree |
| v5.67 fourth-tier evidence expansion — built and run for real | `500` new rows selected, `5/5` batches ready | with `threat_positive_rows` at `79/100` and `real_source_identities` structurally stuck at `1/2`, the project owner explicitly chose (via `AskUserQuestion`) to keep pushing the reachable gate with a `500`-row fourth tier over a smaller `300`-row attempt or dynamic sizing; before writing code, computed the ACTUAL per-coverage-group threat-positive rate across all `1,455` reviewed development rows and found `vendor_security_context` (weighted 3x in v5.65) had produced `0` new threat-positive rows in that round despite `97` sampled — not actually predictive, just correlated in the smaller original sample; new `atdr/app/detection/v567_signal_concentrated_evidence_expansion.py` + review service + CLI, mirroring v562/v563/v565's protocol-lock rigor exactly, with triple-tier (v562+v563+v565) token exclusion and THREE sequential quarantine-then-reaggregate phases; re-calibrated weighting drops `vendor_security_context` and raises `COVERAGE_GROUP_ROUND_ROBIN_WEIGHT` from `3` to `5` on the three groups that actually carry signal (`unknown_transport_context`, `incomplete_transport_context`, `high_activity_context` — 96.2% of all threat-positive rows found so far); also extended `single_source_development_evaluation.py` to include v567's rows; `9` new tests (`7` detection/service + `1` worksheet + `1` single-source-eval); real run against the live source: preflight confirmed the file matches the locked `1,500`-row boundary exactly, then `500/500` rows selected across `5` batches, coverage counts `high_activity_context:138, incomplete_transport_context:133, unknown_transport_context:125` (79.2% of the pack) vs `boundary_context:28, routine_service_context:26, vendor_security_context:25, web_transport_context:25` (~20.8%, each now at parity); worksheet exported (`500` rows, `0` warnings) for the project owner's independent review; see `docs/changes/T1_T20_V5_67_SIGNAL_CONCENTRATED_EVIDENCE_EXPANSION.md` |
| full backend suite (final, whole tree, post-v5.67-build) | pass | `1190 passed, 1 skipped` (`./.venv/Scripts/python.exe -m pytest atdr/tests -q`, 480.48s; from the `1181` prior confirmed baseline: +9 v5.67 tests); Ruff `All checks passed!`; `compileall` exit 0 across the whole tree |
| v5.67 review — genuinely completed by project owner | `500/500` reviewed, `0` invalid, all `5` batches complete | project owner independently hand-reviewed all 500 signal-concentrated rows via the CSV worksheet tool; `36` of the `500` rows were genuinely judged `suspicious`/`malicious`, all from the three re-weighted categories exactly as designed (`unknown_transport_context: 17`, `high_activity_context: 12`, `incomplete_transport_context: 7`; zero from the four de-weighted categories, confirming the recalibration worked); `import_supervised_review_worksheet` reported `submitted: 500, failed: 0` on both a `--dry-run` and the real import; combined v5.62+v5.63+v5.65+v5.67 status now shows `combined_reviewed: 2000/2000`, all four tiers `complete: true` |
| official qualification gates — `threat_positive_rows` now passes | **5 of 6 gates now pass** | `run_v567_signal_concentrated_evidence_expansion --status-only` combined gates: `independent_human_blind_labels: 2000/20 pass`, `independent_comparable_rows: 2000/1000 pass`, `benign_like_rows: 1183/100 pass`, `independent_time_windows: 19/2 pass`, and — closing the gate that has been open all session — `threat_positive_rows: 115/100 pass` (79 prior + 36 new); only `real_source_identities: 1/2` remains fail, and it is permanently structural: no amount of further review of the same physical PAN-OS source can satisfy a 2-source requirement |
| single-source development evaluation — updated with full 1,955-row dataset | executed | `run_single_source_development_evaluation --use-temp-db` against all `1,955` reviewed development-role rows (`1183` benign_like + `115` threat_positive + `657` needs_context, excluded from training): trained on `development_fit` (766 rows), evaluated on `calibration`+`threshold_selection` (532 rows); `accuracy 0.857`, `threat_positive recall 0.938` / `precision 0.381` (precision dropped as the v5.67 tier added many ambiguous `incomplete_transport_context` rows; small-sample per-class metrics, high-variance per the module's own interpretation text); every mandatory disclosure field correct |
| full backend suite (final, whole tree, post-v5.67-import) | pass | `1190 passed, 1 skipped` (unchanged from the pre-import count, as expected — importing review data adds no tests); Ruff `All checks passed!`; `compileall` exit 0 across the whole tree |
| Assistant honest unmatched-question fallback | fixed, 1 defect found | project owner asked for the SOC Assistant to give only necessary info and handle a wider range of questions; investigation (`assistant_service.py`, `assistant_llm.py`, `assistant_response_contracts.py`) found conciseness already well-controlled (55–120 word per-mode budgets, truncation, LLM-oversize rejection) but found a real defect: any question matching none of the ~25 keyword routes fell through to `_answer_general_question`, which answered with an unrelated system-summary paragraph (log/alert counts) regardless of what was actually asked — e.g. asking "what does app_risk mean?" got alert counts back. Fixed by making that fallback state plainly that no dedicated handler matched and echo the analyst's own question, while still surfacing the same safe system-state context; see `docs/changes/T1_T20_ASSISTANT_HONEST_UNMATCHED_QUESTION_FALLBACK.md`. Deliberately deferred, per project owner's own choice: broader keyword-coverage expansion (needs concrete missed-question examples) and a clarifying-questions-back-to-the-analyst feature (real gap — the Assistant never asks the analyst for missing detail today — but a new multi-turn UX feature, not a tuning fix) |
| focused Assistant tests (post-fallback-fix) | pass | `57 passed` (1 new: `test_assistant_chat_admits_when_no_keyword_route_matches_instead_of_a_generic_summary`); Ruff `All checks passed!` on both changed files; `compileall` exit 0 |
| full backend suite (final, whole tree, post-assistant-fix) | pass | `1191 passed, 1 skipped` (`./.venv/Scripts/python.exe -m pytest atdr/tests -q`, 611.73s; from the `1190` prior confirmed baseline: +1 new assistant test) |
| Alert detail white-screen crash — fixed, 1 defect found | fixed, verified against real data | project owner reported every click into an alert's detail panel went to a blank white screen; console error confirmed `Uncaught Error: Objects are not valid as a React child (found: object with keys {field, value})`; traced to `AlertsTriage.tsx` rendering `detectionSummary.observed_evidence` items raw, while the real backend (`build_alert_detection_summary`) has always built that field as `{field, value}` dicts — the frontend's own type declared it `string[]`, so `tsc` never caught the mismatch; confirmed via direct query against the live local database that all real alerts hit this (not an edge case); fixed by normalizing to display strings at the point of consumption and correcting the type; added a backend contract test (11 passed) and extended the Playwright fixture/assertions for the exact deep-link crash path (46 passed, 1 skipped, up from 45); see `docs/changes/T1_T20_ALERT_DETAIL_OBSERVED_EVIDENCE_CRASH_FIX.md` |
| full backend suite (final, whole tree, post-alert-crash-fix) | pass | `1192 passed, 1 skipped` (`./.venv/Scripts/python.exe -m pytest atdr/tests -q`, 494.59s; from the `1191` prior confirmed baseline: +1 new test); Ruff `All checks passed!`; `compileall` exit 0; frontend `tsc --noEmit` and `eslint` clean |
| Governed supervised qualification — final status documented, gate-weakening request declined | closed for this campaign | project owner asked directly whether `real_source_identities: 2` could be waived given `5/6` gates pass and unused source rows remain; declined on the record — the gate is categorical (no row volume from one physical device produces a second device) and weakening it would report `qualified` for a model with no generalization evidence, contradicting every governed-evidence module's own documented invariant; public firewall-log datasets were investigated as a substitute and rejected (no PAN-OS-format real dataset exists publicly; closest candidates are wrong-schema or vendor-synthetic, neither satisfies the gate's intent); `docs/CURRENT_AI_ML_PRODUCT_STATUS.md` (new "v5.62-v5.63 Genuine Review Completion" / "v5.65" / "v5.67" / "Combined Result" / "Second-Source Requirement — Final Decision" sections) and `docs/CURRENT_SYSTEM_STATE_LOCK.md` updated to record the full four-tier campaign result and this decision as the project's canonical final status |

## T5. Blockers And Risks

| ID | Type | Status | Evidence | Impact | Next Action |
| --- | --- | --- | --- | --- | --- |
| R-564-01 | malicious anomaly capture | controlled | best v5.64 diagnostic remains at 50% | no replacement candidate | do not tune the same evidence; require new representation/evidence |
| R-564-02 | rule redundancy | controlled | best queue overlaps rules on 200/202 rows | little independent anomaly value | retain advisory-only role and report novelty separately |
| R-564-03 | temporal drift | controlled | behavior shifts reach 0.64/0.79/1.00 across future roles | static threshold may be brittle | monitor windows; do not consume reserved holdout for tuning |
| R-564-04 | supervised evidence | external | 1,000 selected rows are not reviewed; one source | supervised stays unqualified | complete genuine review and obtain second device later |

## T6. Decision

The v5.64 implementation is complete and its candidate decision is fail-closed.
No anomaly candidate qualified, no holdout was consumed for candidate
selection, and no artifact or authoritative state changed. The full
verification matrix first ran green on 2026-09-18.

A subsequent direct code-review pass (2026-09-19) found and fixed real
defects: two in the uncommitted v5.64 module (a dead duplicate strategy
branch, a legacy-diagnostic schema-key mismatch — neither affected any gate,
ranking, or reported number) and a substring-matching zone-classification bug
that misread Palo Alto's default `untrust` zone as an inside zone. The zone
bug was first found in the published, alert-authoritative rule engine
(`atdr/app/detection/rules.py`, feeding false context into six rules
including the two highest-value ones — C2 beaconing, exfiltration
suspicion) — and a repository-wide search then found the identical defect
independently reimplemented in two more places: the analyst-facing
explanation engine (`explanations.py`) and, most significantly, alert
grouping itself (`services/detection_service.py`'s `_group_key`, which had
zero prior test coverage). All three are fixed by consolidating to one
canonical, now-public implementation, covered by 7 new regression tests, and
documented (v5.64 fixes in
`docs/changes/T1_T20_V5_64_WINDOW_AWARE_ADVISORY_ANOMALY_REDESIGN.md`; the
zone-classification fix in
`docs/changes/T1_T20_RULE_ENGINE_ZONE_CLASSIFICATION_FIX.md`).

Two further pieces of work were completed the same day at the project
owner's direction:

1. **Real, opt-in, host-scoped response enforcement.** The dashboard's
   block/unblock workflow — previously fully simulated — now has a genuine
   Windows Firewall connector (`atdr/app/services/windows_firewall_connector.py`),
   selected deliberately over a real network-firewall connector after review
   of what already existed. It is off by default in every shipped profile,
   requires an explicit two-key opt-in, is refused outside a local/lab
   environment or off Windows, never marks a block active unless enforcement
   genuinely succeeded, never marks an unblock complete unless removal
   genuinely succeeded, always protects the ATDR host's own address(es), and
   supports capped timeouts with lazy expiry sweeping. See
   `docs/changes/T1_T20_RESPONSE_REAL_ENFORCEMENT.md`.
2. **AI Assistant IPv6 redaction gap.** A direct review of the Assistant's
   privacy-critical redaction path found that `IP_PATTERN` was IPv4-only,
   duplicated independently across six files — including two acceptance
   quality-gates that would have silently certified an IPv6 leak as
   privacy-passing. Consolidated to one canonical IPv4+IPv6 pattern. See
   `docs/changes/T1_T20_ASSISTANT_IPV6_REDACTION_FIX.md`.

The complete matrix was re-run in full after every fix and passed: taskboard,
Ruff, compileall, Alembic (including the new additive migration), full
backend suite (`1158 passed, 1 skipped`), frontend lint/build/Playwright
(`45 passed, 1 skipped`), controlled source and layered detection validation
(unchanged safety signature), Assistant QA, replay smoke, performance smoke,
repository surface audit, security acceptance, and the independent release
gate. All four pieces of work (v5.64, the rule-engine fix, real response
enforcement, and the Assistant redaction fix) are approval-ready pending a
separately approved commit/push decision. The configured `.env` was not
modified — response enforcement remains simulation-only by default; the real
connector exists and is tested but must be explicitly enabled to take effect.
